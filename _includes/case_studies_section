<div class="case-studies-section">
    <h2>{{ include.title }}</h2>
    <div class="grid-container">
        {% for case_study in include.id %}
        <div class="card">
            <img src="{{ case_study.image_name | prepend: "/assets/images/casestudy/" | relative_url }}"
                alt="{{ case_study.title }}">
            <h3>{{ case_study.title }}</h3>
            <p>{{ case_study.description }}</p>
            <a href="{{ case_study.url }}" class="btn">Read More</a>
        </div>
        {% endfor %}
    </div>
</div>