<div class="why-choose-us">
    <h2>{{ include.title }}</h2>
    <div class="grid-container">
        {% for reason in include.id %}
        <div class="card">
            {% if reason.image_name %}
            <img src="{{ reason.image_name | prepend: " /assets/images/" | relative_url }}" alt="{{ reason.title }}">
            {% endif %}
            <h3>{{ reason.title }}</h3>
            <p>{{ reason.description }}</p>
        </div>
        {% endfor %}
    </div>
</div>