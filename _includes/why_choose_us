<div class="section why-choose-us">
    <h2 class="section-title">Why Customers Choose Songpoem Labs</h2>
  
    <!-- Flexbox layout for Why Choose Us -->
    <div class="grid-container">
      {% for reason in include.id %}
      <div class="card">
        {% if reason.image_name %}
        <div class="card-image">
          <img src="{{ reason.image_name | prepend: '/assets/mascot/' | relative_url }}" alt="{{ reason.title }}" class="w-12 h-12 mx-auto">
        </div>
        {% endif %}
        <div class="card-content">
          <h3 class="text-lg font-semibold mb-2">{{ reason.title }}</h3>
          <p class="text-sm text-gray-500">{{ reason.description }}</p>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
  